export enum VerifyStatus {
  UNVERIFY = 'unverify',
  PENDING = 'pending',
  SUCCESS = 'success',
}

export enum CampaignStatus {
  FUNDING = 'funding',
  COMPLETE = 'complete',
  TERMINATE = 'terminate',
  FAILED = 'failed',
  DRAFT = 'draft',
}
